group_definitions:
    manufacturer: 'Zhaoxing'

device_definition:
    XH_top_entry:
        series: 'XH'
        mpn_format_string: 'ZX-XH2.54-{pincount:d}PLT'
        orientation: 'V'
        datasheet: 'https://www.lcsc.com/datasheet/lcsc_datasheet_2306191415_Megastar-ZX-XH2-54-2PLT_C7429680.pdf'
        pinrange: ['range', [2, 17]]
        text_inside_pos: -1
        pitch: 2.50
        pad1_position: 'top-left' # 'top-left' | 'bottom-left' -> pin 2 always to the right of pin 1
        mounting_pad_size: [1.5, 3] # From datasheet
        # x position mounting inner mounting pad edge relative to nearest pad center
        center_pad_to_mounting_pad_edge: 2.35 # from datasheet
        # y dimensions for pad given relative to mounting pad edge
        rel_pad_y_outside_edge: 8 # Tuned to achieve 5.5mm long pad
        rel_pad_y_inside_edge: 2.5 # 8.0[datasheet]-5.5[padlen]
        pad_size_x: 1.20 # from datasheet
        # y position for body edge relative to mounting pad edge (positive -> body extends outside bounding box)
        rel_body_edge_y: 0.65 # Estimated
        body_size_y: 5.95
        # x body edge relative to nearest pin
        rel_body_edge_x: 3.75

    VH_top_entry:
        series: 'VH'
        mpn_format_string: 'ZX-VH3.96-{pincount:d}PLT'
        orientation: 'V'
        datasheet: 'https://www.lcsc.com/datasheet/lcsc_datasheet_2306191414_Megastar-ZX-VH3-96-4PLT_C7429652.pdf'
        pinrange: ['range', [2, 11]]
        text_inside_pos: -1
        pitch: 3.96
        pad1_position: 'top-left' # 'top-left' | 'bottom-left' -> pin 2 always to the right of pin 1
        mounting_pad_size: [1.5, 3] # From datasheet
        # x position mounting inner mounting pad edge relative to nearest pad center
        center_pad_to_mounting_pad_edge: 2.7 # tuned
        # y dimensions for pad given relative to mounting pad edge
        rel_pad_y_outside_edge: 10
        rel_pad_y_inside_edge: 4 # 1+mounting_pad_size_y, from datasheet
        pad_size_x: 2.5 # from datasheet
        # y position for body edge relative to mounting pad edge (positive -> body extends outside bounding box)
        rel_body_edge_y: 0.75
        body_size_y: 9.3
        # x body edge relative to nearest pin
        rel_body_edge_x: 3.96
        # additional_drawing:
        #     -
        #         layer: 'F.Fab'
        #         # thickness: 0.1 #optional parameter. If not given: take fab_line_width and silk_line_width from config file. Needed if layer not silk or fab!
        #         reference_point: ['center', -2.5] # [x,y]: x = 'left' | 'center' | 'right' | value; y = 'top' | 'center' | 'bottom' | value; named values are relative to body
        #         rectangle:
        #             size: [0.5, 0.5] # centered around reference_point
        #             # start: [-0.25, -0.25] #relative to reference_point
        #             # end: [0.25, 0.25] #relative to reference_point
        #         # polygon:
        #         #     - [-0.25,-0.25]
        #         #     - [-0.25,0.25]
        #         #     - [0.25,0.25]
        #         #     - [0.25,-0.25]
        #         #     - [-0.25,-0.25]
        #         repeat:
        #             spacing: ['pitch', 0] # x,y= 'pitch' | value
        #             reference_is: 'center' # 'center' | 'first' | 'last'
        #             count: 'pincount' # 'pincount' | value
